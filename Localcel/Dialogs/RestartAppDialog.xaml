<ContentDialog
    x:Class="Localcel.Dialogs.RestartAppDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Restart Required"
    PrimaryButtonText="Restart Now"
    DefaultButton="Primary"
    CornerRadius="8"
    PrimaryButtonStyle="{StaticResource DialogPrimaryButtonStyle}"
    PrimaryButtonClick="RestartAppDialog_PrimaryButtonClick">

    <StackPanel Spacing="12" Width="400" Margin="0,8,0,0">
        <TextBlock x:Name="TxtMessage" TextWrapping="Wrap" Text="Installation complete! Localcel needs to restart to apply system PATH updates."/>

        <Border Background="#252525" CornerRadius="6" Padding="12">
            <StackPanel Orientation="Horizontal" Spacing="10" HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressRing x:Name="RingCountdown" IsActive="True" Width="20" Height="20"/>
                <TextBlock x:Name="TxtCountdown" Text="Restarting Localcel in 10 seconds..." FontWeight="SemiBold" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>
    </StackPanel>
</ContentDialog>
