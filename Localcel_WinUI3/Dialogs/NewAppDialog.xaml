<ContentDialog
    x:Class="Localcel_WinUI3.Dialogs.NewAppDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Create New App"
    PrimaryButtonText="Create App"
    CloseButtonText="Cancel"
    DefaultButton="Primary"
    CornerRadius="8"
    PrimaryButtonStyle="{StaticResource DialogPrimaryButtonStyle}"
    CloseButtonStyle="{StaticResource DialogDefaultButtonStyle}"
    PrimaryButtonClick="NewAppDialog_PrimaryButtonClick">

    <StackPanel Spacing="12" Width="360" Margin="0,8,0,0">
        <TextBlock Text="App Type" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        <ComboBox x:Name="ComboAppType" HorizontalAlignment="Stretch" SelectedIndex="0" SelectionChanged="ComboAppType_SelectionChanged">
            <ComboBoxItem Content="Node.js Server" Tag="node"/>
            <ComboBoxItem Content="Static Page (GitHub Pages)" Tag="static_gh"/>
            <ComboBoxItem Content="Static Page (Cloudflare Tunnel)" Tag="static_cf"/>
        </ComboBox>

        <TextBlock Text="App Name" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        <TextBox x:Name="TxtAppName" PlaceholderText="e.g. my-awesome-app"/>

        <TextBlock Text="Port Number" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        <TextBox x:Name="TxtPort" PlaceholderText="e.g. 3000" Text="3000"/>

        <TextBlock x:Name="LblDomain" Text="Custom Domain (Optional)" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        <TextBox x:Name="TxtDomain" PlaceholderText="e.g. app.mycustomdomain.com"/>

        <TextBlock x:Name="TxtError" Foreground="{ThemeResource SystemFillColorCriticalBrush}" Visibility="Collapsed" TextWrapping="Wrap"/>
    </StackPanel>
</ContentDialog>
